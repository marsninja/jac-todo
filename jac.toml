[project]
name = "jac-todo"
version = "1.0.0"
description = "Jac client application: jac-todo"
entry-point = "main.jac"

[dependencies]

[dependencies.npm]
jac-client-node = "1.0.3"

[dependencies.npm.dev]
"@jac-client/dev-deps" = "1.0.0"

[dev-dependencies]
watchdog = ">=3.0.0"

[serve]
base_route_app = "app"

[plugins.client]
